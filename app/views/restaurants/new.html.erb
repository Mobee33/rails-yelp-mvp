<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.select :category, category: Restaurant::CATEGORY %>
  <%= f.submit %>
  <% end %>
<%= link_to("Back to homepage", restaurants_path) %>
