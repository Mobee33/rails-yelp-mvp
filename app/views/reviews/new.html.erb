<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.select :rating, Rating: Review::RATING %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>

<%= link_to("Back to homepage", restaurants_path) %>
